{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "process_list",
  "description": "Enumerate running processes and their top-level windows.",
  "type": "object",
  "properties": {
    "api_key": {
      "type": "string",
      "description": "API key for authentication."
    },
    "visible_only": {
      "type": "boolean",
      "default": true,
      "description": "If true, only return visible windows."
    }
  },
  "required": [],
  "additionalProperties": false
}
